# Deploy Fix to Shared Hosting Server

Based on your SSH output, you're on a shared hosting environment (node238).

## Current Issues:
1. ✅ Application is running with PM2
2. ❌ server.js hasn't been updated with the fix
3. ❌ Connection timeout when accessing from external IP

## The Fix You Need:

You need to update `/home/nextsril/lu1/server.js` on your server.

### What to Change:

**Find these lines (around line 243-245) in server.js:**
```javascript
app.get('/', isAuthenticated, (req, res) => {
    res.redirect('/dashboard');
});
```

**Replace with:**
```javascript
app.get('/', (req, res) => {
    if (req.session.userId) {
        res.redirect('/dashboard');
    } else {
        res.redirect('/login');
    }
});
```

## How to Deploy:

### Option 1: Using File Manager (Easiest)
1. Log into your hosting control panel (cPanel, Plesk, etc.)
2. Go to File Manager
3. Navigate to `/home/nextsril/lu1/`
4. Edit `server.js`
5. Apply the change above
6. Save file

### Option 2: Using SSH/virtualenv Console
```bash
# You're already logged in, so:
cd /home/nextsril/lu1

# Edit server.js with nano
nano server.js

# Find line 243 and make the change
# Save with Ctrl+X, then Y, then Enter

# Restart PM2
pm2 restart loan-update-system
```

### Option 3: Upload Fixed File
1. Edit server.js locally (already fixed in your workspace)
2. Upload via FTP or File Manager
3. Then SSH and restart:
```bash
pm2 restart loan-update-system
```

## After Deploying the Fix:

### Step 1: Restart PM2
```bash
pm2 restart loan-update-system
```

### Step 2: Check Logs
```bash
pm2 logs loan-update-system --lines 20
```

Look for:
- ✓ "Server is running on port XXXX"
- ✗ Any errors

### Step 3: Find Your Actual URL

On shared hosting, you might not access via IP:PORT directly.
Common patterns:
- https://yourdomain.com:3000
- https://lu1.yourdomain.com
- https://162.214.204.205/lu1
- A subdomain assigned by your hosting

**Check:**
```bash
# Find what port is actually being used
pm2 logs loan-update-system | grep "Server is running"

# This will show: "Server is running on port XXXXX"
```

### Step 4: Test Locally on Server
```bash
# Replace XXXX with the port from above
curl http://localhost:XXXX

# If port is 3000:
curl http://localhost:3000
```

If curl works but browser doesn't, it's a firewall/hosting config issue.

## Finding Your Public URL

Since you're on shared hosting with virtualenv, ask your hosting provider:
1. **"What URL should I use to access my Node.js application on lu1?"**
2. **"Do I need to configure a subdomain or reverse proxy?"**
3. **"Is port 3000 accessible from outside?"**

## Common Shared Hosting Patterns:

### Pattern 1: Direct Port Access
```
http://162.214.204.205:3000
```
Your hosting might require opening this port.

### Pattern 2: Subdomain
```
https://lu1.yourdomain.com
https://lu1.node238.com (or similar)
```

### Pattern 3: Path-based
```
https://yourdomain.com/lu1
```

### Pattern 4: Reverse Proxy
Your hosting might have already set this up and you need to find the URL.

## Quick Diagnostic Commands:

```bash
# 1. Check PM2 status
pm2 status

# 2. Check logs for port number
pm2 logs loan-update-system --lines 50 | grep "port"

# 3. Test local connection
curl http://localhost:3000

# 4. Check if there's a .env file
ls -la | grep env

# 5. Check public directory exists
ls -la public/
```

## Still Having Issues?

Contact your hosting provider support with:
1. Your application directory: `/home/nextsril/lu1`
2. PM2 is running the app
3. You need to know how to access it from a browser
4. Ask them about Node.js application URL configuration

## The Good News:
✅ Your app IS running (PM2 shows it online)
✅ You just need to apply the fix and find the correct URL
✅ The fix is simple - just change 5 lines in server.js

